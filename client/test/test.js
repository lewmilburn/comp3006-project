let chai = require('chai');

describe('Tests', () => {
   describe('Example text',() => {
       it('Example test', () => {
           chai.assert.equal(1, 1, "What went wrong?");
       });
    });
});